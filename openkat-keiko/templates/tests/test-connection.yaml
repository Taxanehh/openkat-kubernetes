apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openkat-keiko.fullname" . }}-test-connection"
  labels:
    {{- include "openkat-keiko.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openkat-keiko.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
