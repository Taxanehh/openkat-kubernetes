apiVersion: v1
kind: Service
metadata:
  name: {{ include "openkat-keiko.fullname" . }}
  labels:
    {{- include "openkat-keiko.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openkat-keiko.selectorLabels" . | nindent 4 }}
