apiVersion: v1
kind: Service
metadata:
  name: {{ include "openkat-bytes.fullname" . }}
  labels:
    {{- include "openkat-bytes.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openkat-bytes.selectorLabels" . | nindent 4 }}
