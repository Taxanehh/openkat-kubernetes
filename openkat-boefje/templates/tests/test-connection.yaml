apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openkat-boefje.fullname" . }}-test-connection"
  labels:
    {{- include "openkat-boefje.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openkat-boefje.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
