apiVersion: v1
kind: Service
metadata:
  name: {{ include "openkat-rocky.fullname" . }}
  labels:
    {{- include "openkat-rocky.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openkat-rocky.selectorLabels" . | nindent 4 }}
