apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openkat-scheduler.fullname" . }}-test-connection"
  labels:
    {{- include "openkat-scheduler.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openkat-scheduler.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
