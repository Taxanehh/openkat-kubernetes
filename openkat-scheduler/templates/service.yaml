apiVersion: v1
kind: Service
metadata:
  name: {{ include "openkat-scheduler.fullname" . }}
  labels:
    {{- include "openkat-scheduler.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openkat-scheduler.selectorLabels" . | nindent 4 }}
