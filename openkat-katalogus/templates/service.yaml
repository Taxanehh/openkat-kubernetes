apiVersion: v1
kind: Service
metadata:
  name: {{ include "openkat-katalogus.fullname" . }}
  labels:
    {{- include "openkat-katalogus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openkat-katalogus.selectorLabels" . | nindent 4 }}
